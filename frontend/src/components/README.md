# Components Directory

Place your React components here. Example: Popup, AnalyticsWidget, DiscordLoginButton, etc.

- Each component should be in its own file (e.g., Popup.tsx).
- Use index.ts to export all components for easy imports.

# Hooks Directory

Place your custom React hooks here. Example: usePopup, useAnalytics, useDiscordAuth, etc.

- Each hook should be in its own file (e.g., usePopup.ts).
- Use index.ts to export all hooks for easy imports.
